[workspace]
members = [
    "crates/dm_core",
    "crates/cli"
, "crates/gui"]

[workspace.dependencies]
clap = { version = "4", features = ["derive"]}

resolver = "2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = "symbols"
